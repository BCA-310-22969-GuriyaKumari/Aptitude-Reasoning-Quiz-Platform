# TODO - Project Image Integration

- [ ] Plan approved: add `projectImg.jpeg` to 4 Thymeleaf pages using `th:src="@{/images/projectImg.jpeg}"`.
- [ ] Update `templates/index.html`: premium centered welcome container + top image banner + role selection buttons/cards.
- [ ] Update `templates/student-entry.html`: 2-column layout with left illustration image + right vertically centered form.
- [ ] Update `templates/admin-login.html`: mirror 2-column layout with left illustration image + right centered admin login card.
- [ ] Update `templates/admin-dashboard.html`: sidebar layout + right content header with full-width banner image (height 120px) + existing stats cards below.
- [ ] Ensure all 4 pages include full-width blue footer fragment `fragments/footer.html :: footer` at absolute bottom.
- [ ] Run quick build/check (optional): `./gradlew test` or `./gradlew bootRun` to ensure templates render.

